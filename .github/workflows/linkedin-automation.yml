name: LinkedIn

on:
  push:
  workflow_dispatch:
  schedule:
    - cron: '*/15 * * * *'

jobs:
  cron-test:
    runs-on: ubuntu-latest

    steps:
      - name: Print Time
        run: |
          echo "Cron works"
          date